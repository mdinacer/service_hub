import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Home: NextPage = () => {
  return (
    <Layout title={''} description={''}>
      <div>test</div>
    </Layout>
  );
};

export default Home;
